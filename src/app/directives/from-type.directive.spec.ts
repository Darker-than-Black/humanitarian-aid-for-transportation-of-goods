import { FromTypeDirective } from './from-type.directive';

describe('FromTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new FromTypeDirective();
    expect(directive).toBeTruthy();
  });
});
